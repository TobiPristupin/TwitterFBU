additional features:
    viewbinding
    UI design
    view automatically shifts up when typing

TODO:
    long names in tweet
    pfp are low res
    hashtags are not blue
    red color if max is exceeded